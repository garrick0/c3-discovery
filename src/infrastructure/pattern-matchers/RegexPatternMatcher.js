export class RegexPatternMatcher {
    async match(graph) {
        return [];
    }
    supports(patternType) {
        return patternType === 'naming';
    }
    getName() {
        return 'RegexPatternMatcher';
    }
}
//# sourceMappingURL=RegexPatternMatcher.js.map